class MathUtils {
  static average(values) {
    if (values.length === 0) {
      return 0;
    }

    return (values.reduce((a, b) => a + b) / values.length).toFixed(1);
  }
}

module.exports = MathUtils;
